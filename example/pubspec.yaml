name: gisila_studio_example
description: Example app demonstrating Gisila Studio admin interface.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.3.3

dependencies:
  gisila_orm:
    path: ../../gisila_orm
  gisila_studio:
    path: ../
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_static: ^1.1.0

dev_dependencies:
  lints: ">=3.0.0 <7.0.0"
  build_runner: ">=2.5.0 <3.0.0"
